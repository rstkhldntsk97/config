spring:
  jpa:
    show-sql: true
  datasource:
    url: jdbc:postgresql://localhost:3306/spring-cloud
    username: postgres
    password: postgres

eureka:
  client:
    sevice-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    prefer-ip-address: true

server:
  port: 8082

